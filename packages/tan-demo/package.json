{
  "name": "@tan/demo",
  "private": true,
  "version": "0.0.1",
  "scripts": {
    "build": "APP_ENV=prod taro build --type weapp",
    "test": "APP_ENV=test taro build --type weapp",
    "dev": "APP_ENV=dev taro build --type weapp --watch"
  },
  "browserslist": [
    "last 3 versions",
    "Android >= 4.1",
    "ios >= 8"
  ],
  "dependencies": {
    "tan": "workspace:*",
    "@tan/common": "workspace:*"
  }
}
